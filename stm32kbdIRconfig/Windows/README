configure IRMP_STM32_KBD

program and read the eeprom (wakeups, macros, irdata, keys and capabilities)
reset wakeups, macros, irdata, keys, alarm and eeprom
set and get the alarm
reboot the device
send IR
