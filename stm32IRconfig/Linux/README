configure and monitor IRMP_STM32_KBD

Usage:
stm32IRconfig [/dev/hidraw1]

program and read the eeprom (wakeups, irdata, keys and capabilities)
reset wakeups, irdata, keys and alarm
set and get the alarm
reboot the device
monitor (read) STM32IR, stop with ^C

put the udev rule from irmplircd into your udev rules directory, then you don't need to give a device
